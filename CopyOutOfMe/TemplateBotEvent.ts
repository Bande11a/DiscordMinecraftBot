/*
Create a folder in MCBOT with your group of commands
Create a .ts file and paste in the code below:
replace the 'EventName' with the name of the event (Object key you send through proccess.send()) 
replace the 'GroupName'
*/


/*
import BaseMCBotEvent from '../../utils/structures/BaseMCBotEvent';
import DiscordClient from '../../client/client';

export default class TestCommand extends BaseMCBotEvent {
  constructor() {
    super('EventName', 'GroupName');
  }

  async run(client: DiscordClient, data: any) {
    console.log("The Bot test command seems to be working");
  }
}
*/
